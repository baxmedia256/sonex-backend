# SONEX Backend — Environment Variables
# Copy this to .env for local development
# Set these as Environment Variables in your Render dashboard for production

# Stripe (from stripe.com → Developers → API keys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Supabase (from supabase.com → Project Settings → API)
SUPABASE_URL=https://kxxozzqvhtjapenrikbb.supabase.co
SUPABASE_SERVICE_KEY=eyJ...  # Service role key (NOT the anon key — this bypasses RLS)

# Frontend URL (your Vercel deployment)
FRONTEND_URL=https://sonex-seven.vercel.app
